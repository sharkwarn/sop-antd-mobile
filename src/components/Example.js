import React from 'react';
import { List, InputItem, Button } from 'antd-mobile';
const Example = () => {
  return (
    <div>
      Example
      <Button>按钮</Button>
    </div>
  );
};

Example.propTypes = {
};

export default Example;
